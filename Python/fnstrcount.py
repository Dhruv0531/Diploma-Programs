# WAP that accepts string and calculate no of characters
def strcount(str):
    count = len(str)
    # for i in str:
    #     count += 1
    print("Number of Characters:", count)


str = input("Enter a String:")
strcount(str)
